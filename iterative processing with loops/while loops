SET SERVEROUTPUT ON
DECLARE  
  num NUMBER := 0;  
BEGIN  
  WHILE num < 20
  LOOP 
    num := num +1;
    IF num = 15 THEN
      CONTINUE;
    END IF;
    dbms_output.put_line(num); 
  END LOOP;  
END;
