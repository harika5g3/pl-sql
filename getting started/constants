
SET SERVEROUTPUT ON
DECLARE
a number(3):= &a;
BEGIN
    IF(a=10) THEN
        dbms_output.put_line('The given number is matched '||a);
    ELSIF(a<20) THEN 
        dbms_output.put_line('The given number is less than 20');
    ELSE
        dbms_output.put_line('The given number is greater then 20');
    END IF;
END;

A Value will not change through out the program.
