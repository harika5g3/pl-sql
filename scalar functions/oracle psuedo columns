CURRVAL and NEXTVAL
LEVEL
ROWID
ROWNUM

SELECT STUDENTS.currval FROM DUAL;
INSERT INTO STUDENT VALUES (STUDENTS.nextval, ‘HARIKA’, ‘PYTHON’, 2001);


SELECT * FROM EMP WHERE ROWNUM <= 2; 

SELECT ROWID, ename FROM emp WHERE deptno = 50;
